# Quick Demos

Short, self-contained demonstrations of technical concepts.

## Notebooks

- [Dilated convolution equivalence](dilated_convolution_equivalence.ipynb) — shows that a 3×3 kernel with one-position gaps (`dilation=2` in PyTorch and TensorFlow) is equivalent to a sparse 5×5 kernel with the intervening weights fixed at zero.
